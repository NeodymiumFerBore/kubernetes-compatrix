import { createContext } from "react";

// https://www.youtube.com/watch?v=MCTB_w0Guso
export const countContext = createContext(null);
export const selectedKubeVersionsContext = createContext(null);
